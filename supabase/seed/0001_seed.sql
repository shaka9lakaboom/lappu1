-- Local development seed, loaded by `supabase db reset`.
-- Intentionally empty at P0: SkillMirror never seeds fake users, courses,
-- evidence or mastery data. Accounts are created through the real signup flow.
select 1;
