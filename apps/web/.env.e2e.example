# Auth E2E test runner only - copy to apps/web/.env.e2e.local (gitignored).
# Read by playwright.config.ts in Node; never loaded by the Next.js app.
SUPABASE_SERVICE_ROLE_KEY=
# Optional: domain for generated test addresses (default example.com)
E2E_EMAIL_DOMAIN=
